# Copy to .env.local for local development (Vite loads it automatically).
# Points the frontend at your backend API.

VITE_API_BASE=http://localhost:8000

# Only needed if your backend has REPO_SENTINEL_API_KEY set (see the
# backend's .env.example) -- leave empty for local dev.
VITE_API_KEY=
